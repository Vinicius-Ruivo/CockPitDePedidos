/**
 * Referência para Power Fx / Dataverse (não importado pelo bundle do PCF).
 * Tabela `Notificacao` — guarda apenas o número de chamado.
 * Ajuste os nomes abaixo se a entidade/coluna no ambiente usar outro prefixo lógico.
 */

/** Nome da tabela no Canvas (display name). */
export const NOTIFICACAO_TABLE_DISPLAY = "Notificacao";

/** Nome lógico da entidade no Dataverse (publisher cr660). */
export const NOTIFICACAO_TABLE_LOGICAL = "cr660_notificacao";

/** Nome lógico da coluna no Dataverse. */
export const NOTIFICACAO_NUMERO_CHAMADO_LOGICAL = "cr660_numerodechamado";

/** Nome da coluna no Canvas (display name da coluna na tabela). */
export const NOTIFICACAO_NUMERO_CHAMADO_DISPLAY = "NUMERODECHAMADO";
